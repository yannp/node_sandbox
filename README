Express generator is installed in the project, not globally. So remember to add ./node_modules/.bin to your $PATH.

Use the foreman package: npm install -g foreman. Create a .env file at the root of the project.

